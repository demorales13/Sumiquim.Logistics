import { ExcelToJsonDirective } from './excel-to-json.directive';

describe('ExcelToJsonDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcelToJsonDirective();
    expect(directive).toBeTruthy();
  });
});
